: data.js has sample data for the website.

: By running index.js we can initialise the database with the sample data given in data.js
                    command : "node index.js" in the init directory